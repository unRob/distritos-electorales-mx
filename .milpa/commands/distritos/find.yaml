summary: finds the districts for given coordinates
description: |
  Reads existing distritos.db and queries for given WGS 84 Web Mercator coordinates
arguments:
  - name: lat
    description: A wgs84 decimal latitude
    required: true
  - name: long
    description: A wgs84 decimal longitude
    required: true
