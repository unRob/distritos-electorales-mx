summary: Imports distritos electorales ito SQLite
description: |
  Uses JQ to turn geojson into SQL, then imports produced SQL into distritos.db in current repo.
options:
  overwrite:
    description: delete existing import file and database
    type: bool
